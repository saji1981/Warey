[build]
  base    = "apps/mobile"
  command = "npx expo export --platform web"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback — all paths served by index.html so deep links don't 404
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
